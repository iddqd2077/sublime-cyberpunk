{
	// Иконки в один цвет — акцент темы (магента #ff2cf1).
	// Чтобы вернуть разноцветные иконки — удали этот файл или поставь "color": "".
	"color": "var(--accent)",

	// Подсветка при наведении — неоновый жёлтый, при выборе — циан.
	"color_on_hover": "#ffd400",
	"color_on_select": "#0ef3ff"
}
